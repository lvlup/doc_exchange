@model DocumentsExchange.DataLayer.Entity.Organization

<div >
    <div id="Tabs" role="tabpanel">
        <!-- Nav tabs -->
        <ul class="nav nav-tabs" role="tablist">
            <li class="active">
                @Ajax.ActionLink("Т1", "Index", "RecordT1", new { id = @Model.Id }, new AjaxOptions() { UpdateTargetId = "tabContent" },
                    new {@role ="tab"})
            </li>
            <li>
                @Ajax.ActionLink("Т2", "Index", "RecordT2", new { id = @Model.Id }, new AjaxOptions() { UpdateTargetId = "tabContent" },
                    new { @role = "tab" })
            </li>
            <li>
                @Ajax.ActionLink("Ф", "Index", "FileCategories", new { id = @Model.Id }, new AjaxOptions() { UpdateTargetId = "tabContent" },
                    new { @role = "tab" })
            </li>
            <li>
                @Ajax.ActionLink("Добавить запись", "CallAddRecord", "Table", new { id = @Model.Id }, new AjaxOptions() {UpdateTargetId = "tabContent"},
                    new {@class = "btn btn-primary", @id = "addRecordBtn"})
            </li>
        </ul>

        <div id="tabContent" role="tabpanel" class="tab-pane active">
             @Html.Action("Index", "RecordT1", new { id = @Model.Id })
        </div>
   
        @*<div class="tab-content" id="tabContent" style="padding-top: 20px">
            <div role="tabpanel" class="tab-pane active" id="table1">
                @Html.Action("Index", "RecordT1", new {id = @Model.Id})
            </div>
            <div role="tabpanel" class="tab-pane" id="table2">
                @Html.Action("Index", "RecordT2", new {id = @Model.Id})
            </div>
            <div role="tabpanel" class="tab-pane" id="files">
                @Html.Action("Index", "FileCategories", new {id = @Model.Id})
            </div>
        </div>*@

    </div>
</div>


@*<script type="text/javascript">
    $(function () {

          $('.nav nav-tabs').closest()
            var tabs = $('#Tabs').tab
        });
</script>*@
