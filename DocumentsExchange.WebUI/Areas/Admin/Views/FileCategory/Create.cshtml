@model DocumentsExchange.DataLayer.Entity.FileCategory

<div class="container">
    <h3>Добавить тип документа:</h3>

    @using (Html.BeginForm("Create", "FileCategory", null, FormMethod.Post, new { id = "AddFileCategoryForm" }))
    {
        @Html.AntiForgeryToken()
        @Html.Partial("_FileCategory")

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-default" id="addFileCategoryBtn">Сохранить</button>
            </div>
        </div>


        @*<div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button  class="btn btn-default" id="deleteFileCategoryBtn">Удалить</button>
        </div>
    </div>*@

    }
</div>

